version: 2

sources:
  - name: sales
    database: "{{ env_var('DBT_DB_NAME') }}"
    schema: sales_schema
    tables:
      - name: sales_agent_incr
      - name: sales_agent_str
      - name: sales_agent_updt_str